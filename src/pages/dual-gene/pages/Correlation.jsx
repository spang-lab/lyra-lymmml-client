import React from 'react';

import { Plot } from 'lyra-library';

const Correlation = () => (
    <div>
        <Plot name="lymmml.correlation.scatterplot" height={600} />
    </div>
);
export default Correlation;
